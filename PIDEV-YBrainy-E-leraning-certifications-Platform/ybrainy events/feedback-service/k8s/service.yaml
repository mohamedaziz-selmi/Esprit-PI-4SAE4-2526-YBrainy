apiVersion: v1
kind: Service
metadata:
  name: feedback-service
spec:
  selector:
    app: feedback-service
  ports:
    - protocol: TCP
      port: 9004
      targetPort: 9004
      nodePort: 30904
  type: NodePort
