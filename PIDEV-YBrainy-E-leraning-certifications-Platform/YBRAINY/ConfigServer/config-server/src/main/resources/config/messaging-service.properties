# ======================================================================
# messaging-service (forum) — Service-specific config
# ======================================================================
spring.application.name=messaging-service
server.port=${MESSAGING_SERVICE_PORT:8184}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/forum_messages?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}

upload.path=uploads/messages
