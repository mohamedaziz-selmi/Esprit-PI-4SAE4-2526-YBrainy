# ======================================================================
# category-service (forum) — Service-specific config
# ======================================================================
spring.application.name=category-service
server.port=${CATEGORY_SERVICE_PORT:8180}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/forum_categories?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
