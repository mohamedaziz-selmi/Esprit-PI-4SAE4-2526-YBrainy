# ======================================================================
# events-user-service (ybrainy events) — Service-specific config
# ======================================================================
spring.application.name=events-user-service
server.port=${EVENTS_USER_SERVICE_PORT:9003}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/user_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
