# ======================================================================
# post-service (forum) — Service-specific config
# ======================================================================
spring.application.name=post-service
server.port=${POST_SERVICE_PORT:8182}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/forum_posts?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
