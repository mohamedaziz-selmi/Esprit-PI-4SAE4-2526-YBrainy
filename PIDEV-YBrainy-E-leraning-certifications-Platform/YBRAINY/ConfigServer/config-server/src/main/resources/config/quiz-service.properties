# ======================================================================
# quiz-service — Service-specific config
# ======================================================================
spring.application.name=quiz-service
server.port=${QUIZ_SERVICE_PORT:8083}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ybrainy_quiz?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
