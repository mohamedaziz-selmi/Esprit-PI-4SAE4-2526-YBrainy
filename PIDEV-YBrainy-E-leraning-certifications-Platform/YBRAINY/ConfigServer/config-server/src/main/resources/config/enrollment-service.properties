# ======================================================================
# enrollment-service — Service-specific config
# ======================================================================
spring.application.name=enrollment-service
server.port=${ENROLLMENT_SERVICE_PORT:8085}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/ybrainy_enrollments?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
