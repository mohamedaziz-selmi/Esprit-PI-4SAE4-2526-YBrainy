# ======================================================================
# forum-engagement-service (forum) — Service-specific config
# ======================================================================
spring.application.name=forum-engagement-service
server.port=${FORUM_USER_SERVICE_PORT:8185}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/forum_users?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
