# ======================================================================
# comment-service (forum) — Service-specific config
# ======================================================================
spring.application.name=comment-service
server.port=${COMMENT_SERVICE_PORT:8183}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/forum_comments?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
